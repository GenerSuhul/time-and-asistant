[build]
command = "pnpm install --frozen-lockfile && pnpm --filter @attendance/web run build"
publish = "apps/web/dist"

[build.environment]
NODE_VERSION = "22"
PNPM_VERSION = "10.14.0"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
