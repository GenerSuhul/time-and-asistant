# Rollback 202607220024

`devices.branch_id` remains the authoritative primary branch throughout this
migration, so older consumers continue to work. Before rollback, deploy the
previous web, `admin-devices`, event-ingestion Edge Function and DeviceGateway
consumers.

Take a fresh database backup, confirm every device has the intended primary
branch in `devices.branch_id`, and then run:

```sql
begin;

drop function if exists public.admin_set_device_branches(uuid,uuid[],uuid);
drop table if exists public.device_branches;

commit;
```

The original pre-migration mapping is retained in `migration_snapshots` under
`migration_key = '202607220024'` and `table_name = 'device_primary_branches'`.
Do not blindly restore that snapshot after administrators have changed primary
branches; it is an audit reference for a point-in-time recovery only.
